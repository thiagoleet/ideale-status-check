import classes from "./MainHeader.module.css";

const MainHeader = (props) => {
  return (
    <header className={classes.header}>
      <h1>My Status Check</h1>
      <nav>
        <ul>
          <li></li>
        </ul>
      </nav>
    </header>
  );
};

export default MainHeader;
